
## TypeScript
- Always use TypeScript (.ts, .tsx) not (.js, .jsx)
- Use `const` / arrow notation over `function`, and `type` over `interface` for declrations
- Rely on implicit return types and minimize casts, keep it simple

## Code style
- No single-line conditionals, always use curly braces
- Do not make or push any commits, I will do that